# ═══════════════════════════════════════════════════════════════════
#  Fairy StackChan —— 开源包
# ═══════════════════════════════════════════════════════════════════
#
# 这是一个「改动集」（patch 包），不是完整项目。
# 使用者需先 clone 上游 xiaozhi-esp32，再把本仓库的文件覆盖进去。
# 详见 README.md / INSTALL.md。

# ── 上游代码本体（使用者自己 clone，不进本仓库）─────────────────
# 本项目只发「改动的文件」，不发上游副本

# ═══════════════════════════════════════════════════════════════════
#  ★★ 美术素材（版权 + 体积，一律不入库）
# ═══════════════════════════════════════════════════════════════════
#  Fairy 等角色形象版权属原版权方（如米哈游）⇒ 绝不提交素材文件。
#  你自己画 / 自己准备的素材，放本地用就行，别 git add。
*.gif
*.png
*.webp
*.jpg
*.jpeg
*.bmp
*.svg
# ★ 例外：代码里内嵌的 LVGL 小图标（是 C 数组，不是美术作品，必须入库）
!firmware/**/assets/*.c
# 表情包目录（使用者自己往里放 GIF —— 那是本地文件）
fairy-assets/
**/fairy-assets/

# ── 大文件（GitHub 单文件硬限 100MB；且音色版权属原声者）──────
# 微调权重：自己训练或用零样本（见 voice-package/README.md）
*.ckpt
*.pth
*.safetensors
*.onnx
*.bin
# ★ 固件走 GitHub Release 附件（不放仓库，避免 clone 拉 11.9MB）
#   下载地址见 firmware-bin/README.md
# ★ 例外：不放任何二进制固件产物
*.elf
*.map

# ── 音频（版权属原声者；不入库）──────────────────────────────
*.wav
*.mp3
*.flac
*.m4a
*.ogg

# ── 敏感文件（绝不能提交）───────────────────────────────────
.env
.env.*
*.key
*.pem
config.local.*
.config.yaml
data/.config.yaml
**/secret*
**/credentials*

# ── 构建产物 / 临时文件 ─────────────────────────────────────
build/
dist/
out/
*.o
*.obj
*.log
*.tmp
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── 编辑器 / 系统 ───────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# ── 备份（本机开发时产生的）─────────────────────────────────
backup/
*.bak
*.old
